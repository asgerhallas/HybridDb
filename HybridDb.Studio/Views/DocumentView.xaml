<UserControl x:Class="HybridDb.Studio.Views.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="141*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,6,0,7" Grid.ColumnSpan="2">
            <ComboBox ItemsSource="{Binding Tables}" SelectedItem="{Binding SelectedTable, Mode=TwoWay}" Width="100" Margin="5,0" DisplayMemberPath="Name"></ComboBox>
            <TextBox Name="DocumentId" Width="300" IsEnabled="{Binding CanFind}" Text="{Binding DocumentId, Mode=TwoWay}" />
            <Button Content="Find" IsEnabled="{Binding CanFind}" Width="75" Margin="5,0"/>
            <Button Content="Save" IsEnabled="{Binding CanSave}" Width="75" Margin="5,0" />
            <TextBlock Text="Working..." VerticalAlignment="Center" Visibility="{Binding Loading, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBox Text="{Binding SelectedDocument.DocumentAsString, Mode=TwoWay}" AcceptsTab="True" Margin="10, 10, 5, 10" TextWrapping="NoWrap" AcceptsReturn="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" />

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Right" VerticalAlignment="Stretch" ResizeBehavior="PreviousAndNext" />

            <DataGrid Grid.Column="2" Margin="5, 10, 10, 10" 
                  ItemsSource="{Binding SelectedDocument.Projections}" 
                  AutoGenerateColumns="False" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Projection" Binding="{Binding Column.Name}" Width="50*" />
                    <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}" Width="50*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StatusBar Grid.Row="2" Height="20">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
